
apply plugin: 'java'
apply plugin: 'eclipse'

sourceSets {
    main {
        java {
            srcDir 'src'
        }
        resources {
            srcDir 'src/resources'
        }
    }
}

dependencies {

    compile fileTree(dir: 'lib', include: '*.jar')
    compile fileTree(dir: 'additions', include: '*.jar')

}

jar {
    manifest {
        attributes 'Main-Class': 'io.github.koxx12_dev.skyclient_installer_java.MainGui'
    }

    from {
        configurations.compile.collect { it.isDirectory() ? it : zipTree(it) }
    }

    exclude 'META-INF/*.RSA', 'META-INF/*.SF', 'META-INF/*.DSA'

}